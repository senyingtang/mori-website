# ---------------------------------------------------------------------------
# 本機開發用：請複製此檔為「.env.local」並填入你的 Supabase 與網址（勿提交 .env.local）
# 複製指令（專案根目錄）：cp .env.local.example .env.local   （Windows 可手動複製改名）
# ---------------------------------------------------------------------------
# 提醒：
# - 不要把 Supabase service_role key 放在 NEXT_PUBLIC_* 變數（會暴露給瀏覽器）。
# - 不要把資料庫密碼放進前端環境變數（NEXT_PUBLIC_* 皆會被打包進客戶端）。
# - .env.local 需自行從本檔複製建立；含真實金鑰的 .env.local 不可 commit 到 GitHub。
# - Vercel 需另外在 Project Settings → Environment Variables 設定這些值（勿將 service_role 設為 NEXT_PUBLIC_）。
# ---------------------------------------------------------------------------

# Supabase Project URL（Dashboard → Settings → API）
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anon（公開）key（Dashboard → Settings → API；僅此種 key 可放 NEXT_PUBLIC_）
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 本機開發建議保持如下；部署 Preview／Production 時請改為對應完整網址
NEXT_PUBLIC_SITE_URL=http://localhost:3000
